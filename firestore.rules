rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Universal read and write access for all app collections
    // Ensures real-time synchronization and permanent cloud persistence across all installs and iterations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
