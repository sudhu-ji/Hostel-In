# Build Project

This workflow explains how to install dependencies and build the Hostel In NextJS project.

1. **Install Dependencies**:
   ```bash
   // turbo
   npm.cmd install
   ```

2. **Build the Project**:
   ```bash
   // turbo
   npm.cmd run build
   ```
