# Preview Project

This workflow explains how to start the local development server for the Hostel In project.

1. **Start Development Server**:
   ```bash
   // turbo
   npm.cmd run dev
   ```

The application will be available at `http://localhost:9002` (or the port specified in the output).
